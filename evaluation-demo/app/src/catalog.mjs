/**
 * Incomplete public-catalog adapter used as the isolated-scenario starting point.
 * Intentionally fails acceptance tests until the Agent applies the reference fix.
 */
export function migrateCatalog(_legacyCatalog) {
  return [];
}
